# Project

Initialized by GitHub Copilot on 2026-06-30.

This repository was scaffolded with a minimal Python starter:

- `src/main.py` — entrypoint
- `requirements.txt` — Python dependencies

Next steps: initialize git and run the app with `python -m src.main`.